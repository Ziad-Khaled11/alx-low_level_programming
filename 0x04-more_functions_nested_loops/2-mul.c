#include "main.h"

/**
 * mul - function prints multiple of two numbers
 *
 * @a: parameter that taken to be checked
 * @b: parameter that taken to be checked
 *
 * Return: multiple of two a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
